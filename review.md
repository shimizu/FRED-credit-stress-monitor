# コード・分析ロジック レビュー結果

- 実施日: 2026-08-10
- 対象ブランチ: `main`
- 対象コミット: `7ec3659`
- 対象: `src/index.js`、`scripts/fetch-fred.js`、`public/data/fred.json`、GitHub Actions、関連ドキュメント

## 結論

アプリケーションは正常にビルドでき、JSONの日付順序・重複・数値形式にも問題はなかった。一方、銀行ストレス指数の構成、総合判定との整合性、CCC/BB変化率比の安定性に、分析結果の信頼性へ影響する問題がある。

データ鮮度については、`git pull --ff-only` 後の `fred.json` が2026-08-09更新、主要系列が2026-08-06まで取得されていることを確認した。以前のローカルデータが古かったのは更新ワークフローの停止ではなく、ローカルリポジトリが未取得だったためである。ただし、将来データ更新が止まった場合に画面上で検知する仕組みはない。

## 最新データの状態

| 系列 | 最終観測日 | 最新値 |
|---|---:|---:|
| HY | 2026-08-06 | 2.71 |
| BB | 2026-08-06 | 1.61 |
| B | 2026-08-06 | 2.87 |
| CCC | 2026-08-06 | 10.17 |
| EMHY | 2026-08-06 | 2.88 |
| TEDRATE | 2022-01-21 | 0.09 |
| CP3M | 1997-08-01 | 5.56 |
| DTB3 | 2026-08-06 | 3.74 |
| SOFR | 2026-08-06 | 3.65 |
| STLFSI | 2026-07-31 | -0.5063 |

## 指摘事項

### P1: 銀行ストレス指数が現在は実質2系列で計算される

[`computeBankStressIndex()`](src/index.js#L610) はTED Spread、CP Spread、SOFR、STLFSIの4系列を想定しているが、日付ごとに存在する値だけを抽出し、2系列以上あれば平均している。

TEDRATEは2022年、CP3Mは1997年で終了しているため、現在の指数はSOFRとSTLFSIだけで算出される。最新データで再計算した結果は次のとおりだった。

- SOFR Zスコア: `0.5068`
- STLFSI Zスコア: `-0.5063`
- BSI: `0.00025`
- Score: `50.00`

画面・ドキュメント上の「4つのZスコア平均」という説明と実際の計算が一致していない。また、構成系列数が時期によって2～4本に変わるため、過去と現在のScoreは同じ尺度にならない。

TEDRATEとCP3MはFRED上でも廃止系列と明記されている。

- [TED Spread (DISCONTINUED)](https://fred.stlouisfed.org/series/TEDRATE)
- [3-Month Commercial Paper Rate (DISCONTINUED)](https://fred.stlouisfed.org/series/CP3M)

さらに、SOFRの絶対水準は金融政策の影響を強く受けるため、そのZスコアをそのまま銀行ストレスとして扱うと、高金利環境をストレスとして評価する可能性がある。SOFRと政策金利・IORBなどとの差、または資金調達スプレッドを使う設計の再検討が必要である。

### P1: 総合シグナルとアラートログが矛盾し得る

[`renderAlerts()`](src/index.js#L731) は次の条件を赤い「警戒」として記録する。

- CCC/BB変化率比が3倍超
- US-EM相関が0.8超、かつ両方のスプレッドが拡大

一方、[`updateOverallSignal()`](src/index.js#L786) はこの2条件を総合シグナルへ含めていない。そのため、アラートログに赤い警戒がある一方で、右上の総合表示が「安定」または「注意」になる可能性がある。

個別判定を1つの共通関数へ集約し、アラートログ、カード、総合シグナルで同じ結果を利用するのが望ましい。

### P1: CCC/BB変化率比が不安定で、改善局面でも警戒できる

[`renderMetrics()`](src/index.js#L357) と [`renderAlerts()`](src/index.js#L755) は、CCCの20日変化をBBの20日変化で割り、絶対値が3を超えると警戒する。

この計算には次の問題がある。

- BBの変化がゼロ付近だと比率が極端に大きくなる。
- CCCのスプレッドが縮小している改善局面でも、絶対値により警戒になる。
- CCCとBBが逆方向に動いた場合も、符号の意味を無視して「パニック」と表示する。

最新データではCCCが`+43bps`、BBが`+2bps`で、比率は`21.5倍`となる。CCCの悪化自体は確認できるが、分母が小さいため比率が過度に増幅されている。

少なくともCCCの変化が正であることを必須とし、分母の最小幅、CCC-BB差の変化量、絶対bps変化を併用すべきである。

### P2: 銀行Scoreの基準値50が「注意」になる

[`bankScoreLevel()`](src/index.js#L659) は45以上を「注意」とする。一方、定義は `Score = 50 + 10 × BSI` なので、BSIが平均的なゼロのときScoreは50となる。

したがって平均的状態が「注意」と判定され、他の指標が正常でも銀行Scoreだけで総合表示が黄色になる。標準スコアとして運用するなら、例えば45～55を正常帯とするなど、基準値とラベルの整合を再検討する必要がある。

### P2: CP Spreadの日付結合で36%の観測値を失う

[`computeCPSpread()`](src/index.js#L579) は月次CP3Mと日次DTB3を同一日付で完全一致させている。実データではCP3M 317件のうち一致したのは202件で、115件、約36.3%が除外された。

月初が休日の場合などを考慮し、直前または直後の営業日のDTB3へ許容日数付きで合わせる必要がある。

### P2: ローリング相関が1観測ずれている

[`rollingCorrelation()`](src/index.js#L127) は結果の日付を `aligned[i]` としながら、計算窓には `aligned[i - 1]` までしか含めない。そのため、最新日として表示される相関は実際には前営業日までの値である。

最新データでは次の差を確認した。

- 現行実装: `-0.0995`（窓の最終日: 2026-08-05）
- 最新日を含む計算: `-0.0608`（窓の最終日: 2026-08-06）

ループ開始位置と `slice()` の終端、または結果へ付与する日付を合わせる必要がある。

また、メトリクスの色は相関が0.8を超えただけで赤になる一方、チャートとアラートは「両方拡大」も条件にしており、表示基準が一致していない。

### P2: データ鮮度の異常を画面で検知できない

現在のデータは最新に更新されており、更新ワークフロー停止の問題は確認されなかった。ただし、[`startFetch()`](src/index.js#L50) は `lastUpdated` を表示するだけで、現在時刻や各系列の最終観測日との差を判定しない。

今後更新が停止した場合も古い値を通常の現在値として表示するため、次の対応が望ましい。

- `lastUpdated` が一定日数以上古ければ警告する。
- 主要系列ごとに最終観測日を検証する。
- 鮮度異常時は総合シグナルを「データ異常」とし、市場判定と区別する。
- 取得スクリプトまたはワークフローで件数・最終日を検証する。

### P2: 「5年」「全期間」が実質的に機能しない

FREDは2026年4月以降、ICE BofA OAS系列について直近3年のみ提供すると明記している。[FRED公式の系列注記](https://fred.stlouisfed.org/series/BAMLH0A0HYM2)

現在のOASデータも約3年分なので、[`5年`と`全期間`の期間ボタン](src/index.html#L600)を選択しても3年以上は表示できない。

長期表示を維持する場合は、取得済みJSONへ新規観測値を追記して履歴を保持する必要がある。保持しない場合は、利用可能期間に合わせてUIと説明を変更すべきである。

### P3: 空配列・不正スキーマへの防御が弱い

データ読み込み後に必須系列、日付、有限数、最低件数を検証していない。欠損系列や、期間フィルター後の空配列に対して `last()`、`reduce()`、`d3.extent()` が呼ばれる箇所があり、描画全体が例外で停止する可能性がある。

取得時と画面読み込み時の両方でスキーマ検証を行い、チャート単位の空状態を表示するのが望ましい。

### P3: ビルド出力が自動消去されない

`npm run build` は成功するが、Viteから次の警告が出る。

```text
outDir .../dist is not inside project root and will not be emptied.
Use --emptyOutDir to override.
```

[`vite.config.js`](vite.config.js#L15) の `build` に `emptyOutDir: true` を明示しない限り、削除済みの古い成果物が`dist/`へ残る可能性がある。

## 検証結果

| 検証 | 結果 |
|---|---|
| `npm run build` | 成功 |
| `node --check src/index.js` | 成功 |
| `node --check scripts/fetch-fred.js` | 成功 |
| JSONの日付順序 | 問題なし |
| JSONの重複日付 | 問題なし |
| JSONの非数値・不正日付 | 問題なし |
| 自動テスト | 未構成 |

## 推奨修正順序

1. 銀行ストレス指数の目的、構成系列、スコア閾値を再定義する。
2. 個別アラートと総合シグナルの判定処理を共通化する。
3. CCC/BB比を、方向と絶対変化量を考慮した安定した指標へ変更する。
4. 相関窓の1観測ずれと表示基準の不一致を修正する。
5. データ鮮度・スキーマ・最低件数の検証を追加する。
6. 3年制限に合わせて履歴保存方法または期間UIを見直す。
7. 計算ロジックをDOM描画から分離し、境界値を含む単体テストを追加する。

## 運用上の確認事項

ICE BofA系列のFRED注記には利用・再配布に関する制限が記載されている。公開リポジトリ内の`fred.json`およびGitHub Pagesでの公開が許諾範囲内か、利用条件を別途確認する必要がある。
